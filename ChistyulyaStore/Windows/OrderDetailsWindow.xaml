<Window x:Class="ChistyulyaStore.Windows.OrderDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Детали заказа" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI">

    <Grid Background="#FFFFFF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#00CED1" Padding="15">
            <StackPanel>
                <TextBlock x:Name="orderTitle" Text="Заказ №" FontSize="20" FontWeight="Bold" 
                           Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock x:Name="clientNameText" FontSize="14" 
                           Foreground="White" HorizontalAlignment="Center" Margin="0,5"/>
                <TextBlock x:Name="orderDateText" FontSize="12" 
                           Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock x:Name="pickUpCodeText" FontSize="12" 
                           Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <ListView x:Name="itemsList" Grid.Row="1" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Товар" Width="250" DisplayMemberBinding="{Binding ProductName}"/>
                    <GridViewColumn Header="Количество" Width="100" DisplayMemberBinding="{Binding Quantity}"/>
                    <GridViewColumn Header="Цена" Width="100" DisplayMemberBinding="{Binding Price}"/>
                    <GridViewColumn Header="Скидка" Width="80" DisplayMemberBinding="{Binding Discount}"/>
                    <GridViewColumn Header="Сумма" Width="100" DisplayMemberBinding="{Binding Total}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Border Grid.Row="2" Background="#F0F0F0" Padding="15">
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Общая сумма заказа:" FontSize="16" FontWeight="Bold" Margin="0,0,10,0"/>
                    <TextBlock x:Name="totalSumText" FontSize="18" FontWeight="Bold" Foreground="#20B2AA"/>
                </StackPanel>
                <Button x:Name="closeButton" Content="Закрыть" 
                        Click="closeButton_Click" 
                        Background="#20B2AA" Foreground="White"
                        Width="100" Padding="10,5" Margin="0,15,0,0"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>